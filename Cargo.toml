[workspace]
resolver = "3"
members = [
    "crr_sb_parser",
    "fpga_arch_parser",
    "fpga_arch_viewer",
]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"

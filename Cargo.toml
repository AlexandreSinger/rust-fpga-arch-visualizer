[workspace]
resolver = "3"
members = [
    "crr_sb_parser",
    "fpga_arch_parser",
    "fpga_arch_viewer",
]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"

# Optimize release builds with Link-Time Optimization
[profile.release]
opt-level = 3
lto = true